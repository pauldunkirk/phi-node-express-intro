var express = require('express'); //npm init creates package.json file
// terminal npm install express creates node modules folder
// npm install --save - this puts the dependencies - don't need to install all node modules
// they can now do npm install - which looks for json and dependencies (see json file)
// 
